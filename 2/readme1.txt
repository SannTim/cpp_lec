auto&& работает, так как std::vector<bool>::reference&& — это корректный тип.
auto& не работает, так как обёртка std::vector<bool>::reference не может быть неявно преобразована в ссылку на bool.

А в общем различие в том, что первом фрагменте x находится копия элемента массива, во втором - ссылка.
